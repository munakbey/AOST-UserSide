#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*|*MINGW*|*MSYS*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/sh" ]; then
  "$basedir/sh"  "$basedir/../@mapbox/cmake-node-module/install.sh" "$@"
  ret=$?
else 
  sh  "$basedir/../@mapbox/cmake-node-module/install.sh" "$@"
  ret=$?
fi
exit $ret
